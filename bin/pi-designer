#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
. "$DIR/lib/peek-env.sh"
exec outfitter run --profile designer --agent pi -- --tools read,grep,find,ls,write,edit,bash,context7_resolve-library-id,context7_query-docs "$@"
