#!/usr/bin/env bash
# Hybrid: outfitter composes skill/model/prompt/extensions; this wrapper hardcodes --tools (the boundary).
set -euo pipefail
exec outfitter run --profile implementer --agent pi -- --tools read,grep,find,ls,write,edit,bash,linear_get_issue,linear_list,mcp_context7_resolve_library_id,mcp_context7_query_docs,mcp_convex_status,mcp_convex_tables,mcp_convex_functionSpec,mcp_convex_data,mcp_convex_run,mcp_convex_runOneoffQuery,mcp_convex_logs "$@"
